#include <iostream>

int main() {
    bool done = false;
    std::string input = "";
    while (!done) {
        //Print Art

        //Wait for command

        //Process Command
    }
    return 0;
}
